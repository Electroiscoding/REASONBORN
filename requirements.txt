# ============================================================================
# ReasonBorn SS-SLM — AMD ROCm Dependencies
# Install via: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/rocm6.1
# ============================================================================

# --- Core ML Framework (ROCm wheels resolved via --extra-index-url) ---
# torch==2.1.2
# torchvision==0.16.2
# torchaudio==2.1.2

# --- Training & Orchestration ---
transformers
datasets
accelerate
# deepspeed==0.11.0
wandb
tensorboard

# --- Numerical & Scientific ---
numpy
scipy
scikit-learn
pandas
tqdm
sympy

# --- Formal Verification ---
z3-solver

# --- Vector Search (CPU-based FAISS, compatible with all backends) ---
faiss-cpu

# --- Embeddings & NLP ---
sentence-transformers
datasketch

# --- Privacy ---
opacus

# --- Serving ---
fastapi
uvicorn
pydantic
pyyaml

# --- ROCm Flash Attention (install separately via Dockerfile or manually) ---
# pip install git+https://github.com/ROCm/flash-attention.git@main

# ============================================================================
# REMOVED NVIDIA-ONLY PACKAGES:
#   - nvidia-cublas-cu12, nvidia-cuda-runtime-cu12 (not applicable on ROCm)
#   - faiss-gpu (NVIDIA CUDA-only; use faiss-cpu or compile faiss from source with ROCm)
#   - torch-tensorrt (NVIDIA TensorRT; use MIGraphX for AMD inference optimization)
# ============================================================================
